# Example Ansible inventory.
# Copy this file to inventory.ini and replace placeholders locally.
# Do not commit real private IPs, usernames, hostnames, or key paths.

[monitoring]
# lab-node-01 ansible_host=example.local

[exporters]
# lab-node-02 ansible_host=example.local

[all:vars]
# ansible_user=example-user
# ansible_ssh_private_key_file=~/.ssh/example_key
