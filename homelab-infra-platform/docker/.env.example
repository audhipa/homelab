# Example environment file for Docker Compose.
# Copy this file to .env locally and change the values there.
# Do not commit real passwords or tokens.

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-locally
